#include <iostream>
#include <math.h>
#include <stdio.h>

using namespace std;
void test(int &x) { x = 12; }
int main(int argc, char const *argv[]) {
  int x = 1;
  test(x);
  printf("之后的x: %d\n", x);
  cout <<(long) pow(2, 24);
  ;
  cout << 1;
  return 0;
}
