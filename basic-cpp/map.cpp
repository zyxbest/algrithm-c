#include <bits/stdc++.h>
using namespace std;
int main(int argc, char const *argv[]) {
  map<int, bool> visited;
  // 没访问过就为false, 访问过, 再把它设为true
  visited.contains(1);
  cout << !visited[3] << endl;
  cout << __cplusplus;
  return 0;
}
